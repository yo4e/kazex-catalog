# AGENTS.md

このリポジトリは、KAZEX Records の公開カタログを人間とAIで共同運用するためのものです。

ChatGPT、Codex その他のAIエージェントは、作業前にこのファイルと `README.md` を読み、対象タスクに応じて `docs/` の仕様を参照してください。

## 最重要ルール

### 1. PUBLIC DATA ONLY

このリポジトリは公開リポジトリです。

以下をコミットしないでください。

- パスワード
- APIキー、アクセストークン、秘密鍵
- 個人の非公開連絡先
- 契約上の秘密
- 公開前に秘匿する必要がある情報
- 外部サービスの認証情報
- Google Drive 等の非公開共有リンクで、公開すると問題があるもの

判断に迷う情報は、公開しない側に倒してください。非公開情報が運用上必要になった場合は、別の private リポジトリまたは適切な秘密管理先へ分離する案を提示してください。

### 2. 事実を捏造しない

Spotify URL、UPC、ISRC、リリース日、受賞歴、チャート実績、再生数、プレス掲載、ツアー、広告施策など、不明な情報を推測で埋めないでください。

不明値は `null`、空配列、または仕様上許される未確定状態として残してください。

### 3. 正本を二重化しない

同じ事実を複数ファイルへ手入力で重複させることは、できるだけ避けてください。

例：リリース日は `releases/<release-id>.yaml` を正本とし、サイト用データやPitch用文章が必要なら、そこから派生させる設計を優先します。

## YAML運用

- インデントはスペース2個を基本とする
- キー名は既存のものを優先し、似た意味のキーを勝手に増殖させない
- 長文は `>` または `|` を使う
- 日付は原則 `YYYY-MM-DD`
- IDはURLやパスにも使える小文字 kebab-case を基本とする
- 不明な値を空文字で埋めず、可能なら `null` を使う
- 数字に見える識別子（UPC等）は文字列として扱う

## ファイル構成

- `artists/*.yaml` — アーティスト単位の公開情報
- `releases/*.yaml` — リリース単位の公開情報とプロモーション情報
- `docs/` — 仕様・将来設計
- `templates/` — 新規登録時の雛形

## Priority Pitch

Too Lost Priority Pitch を作成・更新するときは、必ず `docs/PRIORITY_PITCH.md` を参照してください。

Pitch本文やMilestoneは事実に基づき、英語として自然に整えて構いませんが、実績・施策・受賞歴を盛らないでください。

## Google Drive素材管理 / Inbox

素材を扱う作業では `docs/ASSETS.md` を参照してください。

KAZEX Records Assets の直下には `Inbox/` があり、人間が未整理素材を雑な仮名のまま投入してよい運用になっています。

AIが素材整理を担当する場合は、可能であれば最初に `Inbox/` を確認してください。

- ファイル名、内容、既存カタログ、Drive構造から所属が十分に判断できる場合だけリネーム・分類する。
- アーティスト写真、ロゴ、EPK、リリースのCover Art / Visuals / Other Assetsなど、用途に応じた既存フォルダへ移す。
- 正式採用素材としてカタログから参照すべき場合は、対応するartist / release YAMLも更新する。
- どのアーティスト／作品／用途か判断できない場合は推測しない。山田へ確認し、確認前は `Inbox/` に残す。
- 不明ファイルを「たぶんこれ」と勝手に分類しない。
- 内容を確認せず削除・上書きしない。

InboxのURLや詳細な命名・分類ルールは `docs/ASSETS.md` を正本とします。

## 将来のWebサイト連携

このリポジトリは将来、KAZEX Records公式サイトのデータソースになる予定です。

新しい構造を追加するときは、以下を意識してください。

- 人間が直接読める
- AIが安定して更新できる
- スクリプトから機械的に検証できる
- Webサイトから必要な公開情報だけ抽出できる
- 将来SQL等へ移行しても意味構造を保てる

現在の将来設計は `docs/ROADMAP.md` を参照してください。

## 変更時の原則

小さな変更では既存構造を尊重してください。大規模なデータモデル変更、自動化基盤の導入、SQL移行などは、既存データとの互換性と移行手順を先に文書化してから実装してください。
